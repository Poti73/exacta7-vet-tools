import { LocalHistory } from '../../components/local-workspace';

export const metadata = { title: 'Historial local', robots: { index: false, follow: true } };
export default function LocalHistoryPage() { return <LocalHistory />; }
