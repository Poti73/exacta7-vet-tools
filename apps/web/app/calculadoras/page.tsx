'use client';
import Link from 'next/link';
import { publishedCalculators } from '@exacta7/clinical-core';
import { useI18n } from '../../components/i18n';
export default function Page(){const{locale}=useI18n();const t=locale==='en'?{eye:'TOOLS',title:'Clinical calculators',lead:'Deterministic operations, visible units and values selected by you.',pro:'Pro'}:locale==='fr'?{eye:'OUTILS',title:'Calculateurs cliniques',lead:'Opérations déterministes, unités visibles et valeurs choisies par vous.',pro:'Pro'}:{eye:'HERRAMIENTAS',title:'Calculadoras clínicas',lead:'Operaciones deterministas, unidades visibles y valores elegidos por ti.',pro:'Pro'};return <><div className="page-intro compact"><p className="eyebrow green">{t.eye}</p><h1>{t.title}</h1><p className="lead">{t.lead}</p></div><div className="tool-grid">{publishedCalculators.map((calculator,index)=><Link className="tool-card" href={`/calculadoras/${calculator.slug}`} key={calculator.id}><span className="tool-number">{String(index+1).padStart(2,'0')} {calculator.plan==='pro'&&<em className="tag">{t.pro}</em>}</span><h2>{calculator.name[locale]}</h2><p>{calculator.description[locale]}</p><span aria-hidden>→</span></Link>)}</div></>}
