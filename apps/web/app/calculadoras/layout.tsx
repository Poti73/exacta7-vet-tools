import { publicMetadata } from '../../lib/seo';

export const metadata = publicMetadata('/calculadoras', 'Calculadoras veterinarias transparentes', 'Calculadoras veterinarias con fórmula, unidades y pasos visibles. Los valores clínicos se seleccionan o verifican por el profesional.');

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
