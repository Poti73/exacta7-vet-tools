import { publicMetadata } from '../../lib/seo';

export const metadata = publicMetadata('/contacto', 'Contacto', 'Contacta con Exacta7 para soporte técnico, consultas sobre datos regulatorios o colaboración.');

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
