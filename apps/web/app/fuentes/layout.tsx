import { publicMetadata } from '../../lib/seo';

export const metadata = publicMetadata('/fuentes', 'Fuentes y metodología', 'Cómo Exacta7 separa datos regulatorios AEMPS/CIMA Vet, evidencia clínica revisada y cálculo matemático determinista.');

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
